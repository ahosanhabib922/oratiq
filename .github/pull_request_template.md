## What does this change?

<!-- One or two sentences. -->

## Checklist

- [ ] `npm run build` passes locally
- [ ] Verified in **light and dark**
- [ ] Verified in **LTR and RTL** (header toggle)
- [ ] Only logical properties used (no `left-`/`right-`/`ml-`/`pr-`)
- [ ] Demo/docs updated if the component's API changed
